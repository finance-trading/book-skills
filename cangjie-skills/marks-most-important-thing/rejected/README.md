# Stage 1.5 淘汰与合并记录

原始候选仍完整保留在 `candidates/`。本文件记录没有独立成 skill 的处理规则。

## 合并

- `f01-f02`、相关 `principles` 和 `cases` 合并为 `second-level-thinking`
- `f04-f06` 合并为 `price-value-separation`
- `f07-f08` 合并为 `permanent-loss-risk`
- `f18` 与安全边际相关原则合并为 `margin-of-safety`
- `f05/f09` 合并为 `probability-distribution`
- `f11` 合并为 `cycle-positioning`
- `f06/f12` 合并为 `pendulum-psychology`
- `f13/f14` 合并为 `contrarian-investing`
- `f14-f16` 合并为 `price-first-patience`
- `f17` 合并为 `defensive-investing`
- `f19` 合并为 `correlation-diversification`
- `f21-f22` 合并为 `circle-of-competence`
- `f23` 合并为 `reasonable-expectations`
- `f10/f26/f27` 合并为 `luck-skill-attribution`

## 降级或淘汰

- 只在单一章节出现的具体判断：降级为案例或引用，V1 不足。
- 只复述单一历史事件的候选：降级为 A1/B 段素材，V2 不足。
- “保持理性”“要有耐心”等无机制格言：降级为执行规则，V3 不足。
- 单独的公司、危机和市场故事：不独立成 skill，保留在 `candidates/cases.md`。
- 重复的原则、反例和术语：合并入对应 skill，不重复建 skill。
